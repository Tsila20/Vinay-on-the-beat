{
  "name": "vinay-on-the-beat-release-firebase",
  "version": "4.0.0",
  "private": true,
  "description": "Release-ready Capacitor Android app with Firebase chat, app icon, splash screen, and signed APK/AAB GitHub workflow.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "android:sync": "npm run build && npx cap sync android",
    "android:add": "npx cap add android",
    "android:open": "npx cap open android",
    "assets:generate": "node scripts/generate-assets.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2",
    "@capacitor/push-notifications": "^6.0.2",
    "@capacitor/splash-screen": "^6.0.2",
    "firebase": "^10.13.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2",
    "vite": "^5.4.2"
  }
}
